{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/node",
            "config": {
                "installCommand": "yarn install",
                "buildCommand": "yarn migrate && yarn build"
            }
        }
    ]
}